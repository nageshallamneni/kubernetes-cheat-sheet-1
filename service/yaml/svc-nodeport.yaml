apiVersion: v1
kind: Service
metadata:
  namespace: webapps
  name: nginx-service-nodeport
spec:
  selector:
    app: nginx-web-apps
  ports:
  - port: 8888
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  type: NodePort